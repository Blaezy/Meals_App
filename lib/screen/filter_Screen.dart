import 'package:flutter/material.dart';

class FiltersScreen extends StatelessWidget {
  static const routeName = '/Filter-Screen';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Filtered Items'),
    );
  }
}
